<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shaun.chauma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminScreen" />
        <activity android:name=".BrochureScreen" />
        <activity android:name=".ClinicScreen" />
        <activity android:name=".EditAccountScreen" />
        <activity android:name=".EventScreen" />
        <activity android:name=".FAQ" />
        <activity android:name=".GetInvolveScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".PeerEducatorScreen" />
        <activity android:name=".SetEventScreen" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>