<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shaun.chauma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".BrochureActivity" />
        <activity android:name=".ClinicActivity" />
        <activity android:name=".EditAccountActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".FrequentlyAskedQuestionActivity" />
        <activity android:name=".GetInvolveActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PeerEducatorActivity" />
        <activity android:name=".SetEventActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactActivity" />
        <activity android:name=".AskQuestionActivity"></activity>
    </application>

</manifest>